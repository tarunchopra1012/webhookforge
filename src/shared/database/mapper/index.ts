/**
 * Mappers live here, one file per aggregate: `{entity}.mapper.ts`, exporting
 * a class with static `toDomain()` / `toPersistence()`.
 *
 * Empty until the first entity lands. Re-export each one from this barrel as
 * it is added.
 */
export {};
